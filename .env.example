# Copy this file to .env and fill in real values.
# .env is gitignored and should never be committed.

# Required once the model-backed node is wired in.
OPENAI_API_KEY=

# Optional model override for local runs.
OPENAI_MODEL=gpt-4o-mini

# Optional MCP server config for MCP-connected graph demo.
MCP_SERVER_NAME=weather
MCP_TRANSPORT=http
MCP_SERVER_URL=http://localhost:8000/mcp
# Optional auth header/token pair for MCP server calls.
MCP_AUTH_HEADER=
MCP_AUTH_TOKEN=

# Optional LangSmith tracing.
LANGSMITH_API_KEY=
LANGSMITH_TRACING=false
LANGSMITH_PROJECT=langgraph-persistence-skeleton
